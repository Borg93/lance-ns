{
	"name": "@lance/config",
	"version": "0.1.0",
	"private": true,
	"description": "Shared workspace presets (tsconfig) — apps/* and packages/* extend these so compiler strictness is set ONCE (rask convention: a config package, not copy-pasted options)."
}
