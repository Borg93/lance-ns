<script lang="ts">
	// apps/web is being retired: every governance surface now lives in a micro-frontend zone
	// (/data, /lineage, /models, /admin) served through the microfrontends proxy. This root remains
	// only so the legacy app builds during the migration; it is deleted in P5.
</script>

<svelte:head><title>lance</title></svelte:head>
<p style="padding:2rem">The lance UI moved to the micro-frontend zones.</p>
