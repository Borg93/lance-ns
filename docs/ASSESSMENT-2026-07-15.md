# Assessment 2026-07-15 — catalog bench, rask-merge readiness, production readiness

Three parallel assessments (multi-agent, evidence-grounded in this repo + web currency checks),
requested alongside the #42 model-registry UI ship. Companion to `FEATURE-GAP.md` (the standing
Lakekeeper/Marquez bench); this file adds the wider-catalog bench and the two forward-looking work-lists.

## 1. Feature bench vs Polaris / Unity Catalog OSS / Gravitino / Nessie / Lakekeeper

> Update 2026-07-16 (later): bench item #2 (table-maintenance policy store) SHIPPED as #50 — REST
> policy CRUD (owner-gated, audited) + sweep enforcement with native tag-pin protection.
> Update 2026-07-16: bench item #1 (business-metadata/tag write API + description) SHIPPED as #49 —
> governed writes on the lineage service + browse-UI editing; the ranked list below starts at item #2.


# Bench: lance-ns vs Polaris / Unity Catalog OSS / Gravitino / Nessie / Lakekeeper (2026-07-15)

**Ground truth**: `/home/blackwell/Desktop/lance-ns/docs/FEATURE-GAP.md` (Lakekeeper + Marquez bench, currency-checked 2026-07-12) and `/home/blackwell/Desktop/lance-ns/docs/API.md` (66 catalog + 21 lineage paths). Claims verified in `services/catalog` and `services/lineage` where noted.

**Currency check**: Lakekeeper's latest release is still v0.13.1 (2026-06-30) — nothing shipped after FEATURE-GAP.md's 2026-07-12 check, so its Lakekeeper section remains current (v0.13.0's Lance generic tables, nested roles, admission gates, multi-OIDC, per-warehouse format-version policy are all already accounted for). Competitor states used: Polaris 1.5.0 (2026-05-18: generic tables GA, catalog federation incl. BigQuery Metastore, policy store, multi-event-listener framework, OPA + Ranger external authorizers), Unity Catalog OSS 0.4.x–0.5.0 (storage credentials + external locations + vending, VARIANT, atomic CTAS, metric views experimental, models/volumes/functions; note: the web fetcher rendered some UC dates as 2024 — the CVE-2026-27478 fix in 0.4.1 confirms these are 2026 releases), Gravitino 1.2–1.3 (Table Maintenance Service, metalake-wide tags incl. column tags, policies + task templates, UDF management, redesigned UI, **a Lance REST service since 1.1**), Nessie (catalog-level git semantics — stable feature set).

One doc staleness found: FEATURE-GAP.md §3 says full-text search is "deferred", but tier-1 governed metadata search **shipped 2026-07-11** (`services/lineage/api/v1/endpoints/discovery.py:48` — case-insensitive substring over names/namespaces/tags/columns, FGA-filtered before limit). Only tier-2 (Lance FTS/vector) remains deferred.

## Genuinely missing and valuable

| Feature | Who has it | What they have | Us today | Why it matters here |
|---|---|---|---|---|
| **Business-metadata / tag management API** | Gravitino (metalake-wide tags down to columns, tag-based browse), UC (comments on tables/columns, properties), Lakekeeper (generic-table free-form metadata) | User-writable tags/descriptions on any object, searchable | Two read-only halves, no write path: Lance version-ref tags (`/v1/table/{id}/tags/*` — refs like `blessed`, not labels) and lineage governance tags ingested only from producer OpenLineage `tags` facets. `/search` already indexes tags but users can't set them | Cheapest high-leverage gap: `/search` + `/datasets?tag=` already consume tags; a governed `PUT /datasets/{name}/tags` + description field completes discovery. Today a human can't mark a table "pii" or "deprecated" without emitting a fake lineage event |
| **Declarative table-maintenance policy store** | Polaris (policy store: compaction/snapshot-expiration/retention policies, REST lifecycle, inheritance), Gravitino (TMS: policy-driven scheduled maintenance, task templates) | Per-table/namespace policies the maintenance engine reads | A capable executor with no policy plane: separate `compaction` service (Dapr/NATS-driven, FAIL RunEvents). No per-table policy API for compaction cadence or **old-version cleanup/retention** | Lance-specific pain: every write creates an immutable version; without a retention/cleanup policy, storage grows forever. FEATURE-GAP §2 already flags "unified maintenance scheduler missing"; both competitors now model this as *policies*, not just jobs. We'd reuse the existing compaction service as the executor |
| **Outbound catalog federation / generic-table registration** | Polaris (federation GA: Iceberg REST, BigQuery Metastore, `FederatedCatalogFactory`), Gravitino (federated metalake + **speaks lance-namespace REST since 1.1**), Lakekeeper 0.13 (catalogs Lance tables as generic pointers) | A single org catalog can project/govern tables living elsewhere | Nothing — lance-ns is an island. FEATURE-GAP §2 itself flags "possible future interop: registering our tables as their generic pointers" | The ecosystem converged on generic-table pointers in H1-2026; a small sync worker registering our tables into Lakekeeper/Polaris/UC (or serving Gravitino's lance-namespace dialect) gives orgs one catalog view **without ceding our data plane** — the exact "complementary not competitive" position FEATURE-GAP argues |
| **Access-review tooling ("who can access X")** | Lakekeeper (Console permission management, role-in-role introspection), Gravitino (authorization UI over tags/jobs/policies), Polaris (grants listing APIs) | Enumerate grants per object/principal, manage them in a UI | Verified in code: `fga.list_objects` is used only for *my*-visibility filters (`tables.py:78`, `models.py:132`, `warehouses.py:167`); `fga.list_users` (who-has-relation-on-object) is never called. Grants management = operator-only `.localbin/fga` CLI; audit trail (#41) logs decisions but can't answer standing-access questions | Our ReBAC is finer-grained than any competitor's in the data path, but nobody — including an auditor — can ask "who can read `gold/features`?" through the API or UI. One `list_users`-backed endpoint + a grants panel converts the FGA moat into reviewable governance |
| **Admin/console UI scope** | Lakekeeper Console (warehouses, tables, permissions), Gravitino redesigned Web UI (catalogs, tags, policies, task templates), UC UI | Full catalog administration in the browser | ✓ Closed by #52 (2026-07-16): the SvelteKit app now has `/` browse, `/lineage` explorer, `/models`, `/tables` + `/tables/[id]` (schema/versions/tags/stats + owner-gated policy edit + access review), and `/warehouses` (provision/activate/deactivate/bind); server-side FGA-filtered `/search` wired into browse | Was UI-orphaned; now surfaced through session-only BFF routes with correct sign-in/data states per stack mode. Remaining console gaps are cosmetic (bulk ops, richer dashboards), not capability |
| **Catalog change-events / webhook framework** | Polaris 1.5 (multiple event listeners, per-event-type opt-in), Lakekeeper (CloudEvents to NATS/Kafka) | Generic, configurable catalog-change event fan-out | Partial (verified): table lifecycle + writes emit OpenLineage over Dapr/NATS + HTTP (`core/lineage_emit.py`, drop emits too — `tables.py:205`); audit events over OTLP. Not covered: grants/tuple changes, warehouse lifecycle, namespace ops as subscribable events | Lower urgency because OpenLineage-over-NATS already *is* our event bus for data changes; the gap is only non-data control-plane events (grant changed, warehouse deactivated) for cache-invalidation/sync consumers |
| **Catalog-level branching / rollback** | Nessie (branch/tag/rollback the *entire catalog*, zero-copy environments) | Git-for-data across all tables at once | Per-table branches/tags/time-travel (Lance-native) **plus** atomic multi-table `POST /v1/table/batch-commit` (verified native `batch_commit_tables`, `versions.py:57`) — which is most of Nessie's headline | Only the "whole-catalog zero-copy dev environment" remains missing; heavy to build over a directory namespace and per-table refs + batch-commit cover the realistic Lance workflows. Park |
| **Multi-storage-profile + SSE-KMS/remote-signing vending** | Lakekeeper (SSE-KMS, remote signing), UC (external locations + storage credentials as first-class objects), Polaris | Several storage roots/credentials per warehouse; KMS-encrypted vending | One profile per warehouse (#3-A/#35); vending ModeB/Static/STS/WebIdentity with per-table session policies (`core/vending.py`) | Already marked "on par, SaaS-scale only" in FEATURE-GAP — still true after this bench |

## Already have (verified — no action)
- **Credential vending**: on par with UC 0.4 / Polaris (per-table expiring creds, `/credentials` + `/commit` client-direct write; zero data bytes through the catalog).
- **Multi-warehouse physical tenancy** (#3-A + #35), soft-delete/undrop (stronger than Polaris/Lakekeeper minus timed expiration), per-table branches/tags/versions/time-travel, **atomic multi-table commit** (`batch-commit`), governed metadata **search tier-1** (shipped 2026-07-11, doc stale), discovery lists, model registry (#17/#42 — UC's registered-models equivalent, validator-gated promotion), blob-v2 multimodal + governed Range serving (our answer to UC volumes), table stats, maintenance mode, audit trail (#41), OpenLineage event stream.
- **Lineage**: still the moat — none of the five (Polaris events ≠ lineage; Gravitino's capture is nascent; Lakekeeper has zero) has versioned, storage-reconciled, column-level, FGA-governed lineage.

## N/A-by-design (do not build)
- Iceberg REST facade / Iceberg commit coordination (we operate Lance, not Iceberg pointers); functions/UDF management (UC, Gravitino — no SQL engine here); UC volumes as ungoverned file dirs (blob-v2 in-table is the deliberate alternative); OPA/Ranger/Cedar external-authorizer pluggability (OpenFGA is the committed engine, non-substitutable per the k8s/Dapr audit); geo-distributed metalake (Gravitino — wrong scale); inbound federation of Hive/JDBC/ClickHouse catalogs (we are a lakehouse catalog, not a metadata lake); presigned URLs (governed proxy chosen deliberately — signed URLs bypass ReBAC); multiple OIDC providers (Dex itself federates IdPs); metric views / semantic layer (blocked on native Lance MV/`base_objects`, already tracked); UC VARIANT type (Arrow-native types cover it).

**Bottom line**: no *obvious* table-stakes hole — vending, tenancy, refs, soft-delete, search, registry, and events are all present and lineage exceeds every competitor. The real gaps are governance-plane conveniences the 2026 crop converged on: writable business metadata, policy-driven maintenance (Lance version retention especially), generic-table interop, access review, and console breadth.

## 2. What is left before the rask merge

# What's left before the rask merge — concrete work-list

Grounding: `/home/blackwell/Desktop/lance-ns/docs/RASK-INTEGRATION.md` (the migration checklist), `docs/OPERATORS.md` (adoption order + the Ray submit-seam decision), `docs/DESIGN-catalog-parity.md` (archive; #1–#5 all shipped 2026-07-15, so the *feature* prerequisites for the merge are done), and a direct read of the rask checkout at `/home/blackwell/Desktop/lance-ns/rask` (gitignored sibling, HEAD `4f0d22d`). Nothing merge-blocking remains on the lance-ns feature side; everything left is integration mechanics plus five decisions.

## 1. What rask provides that we inherit (use, don't rebuild)

- **Operators, all already run by rask's one umbrella chart** (`rask/chart/Chart.yaml`): KubeRay + Kueue (with queue topology in `templates/kueue-queues.yaml`), CloudNativePG (`cnpg-cluster.yaml` → `<release>-postgres-rw`), rustfs-operator (`rustfs-tenant.yaml`, declarative `buckets:` list), NATS 2.14.2, Dapr 1.18.1, OpenFGA server 0.3.9, GreptimeDB/Vector/Perses, Traefik ingress (`templates/ingress.yaml`), nvidia-device-plugin. Our hand-rolled AGE StatefulSet/RustFS Deployment/raw Ray head all die here.
- **Ray compute patterns**: `packages/ray-kit` (Job SDK + dashboard wrapper), `components/services/ray_api` (`/api/ray/*` + `/api/serve/*`), `components/cli/runner` (Ray Data pipelines), `RayService` CR + Kueue admission. Per `docs/OPERATORS.md` §3 the decided seam: lance-ns keeps the agnostic Jobs-REST signatures; rask supplies the `RayJob`-CR transport behind them. Do *not* adopt ray-kit into our services or port our submit logic into rask.
- **Lance FTS search, already in production shape**: `components/services/search_api` — LanceDB `AsyncTable.query().nearest_to_text()` over `s3://<bucket>/lines`, S3 thumbnail proxy with key-prefix defense, lines-only lifespan. This is the pattern for our deferred lineage `/search` (hybrid+rerank was our only noted lancedb-SDK gap).
- **Frontend MFE shell**: 7 SvelteKit SSR apps under `components/frontends/` (vertical/route-based MFEs, *not* Module Federation), each pinned to `kit.paths.base=/default/<domain>`, composed by Turborepo's built-in `:3024` proxy in dev (`microfrontends.json`) and ingress path-routing in prod (`frontend.apps` list in `chart/values.yaml` → `templates/frontends.yaml` + `ingress.yaml`). Shared chrome = `@rask/ui/shell` (`AppShell`/`AppSidebar`/nav-config), shared client = `@rask/api` with `makeGatewayHandleFetch` for SSR. One parametrized `.docker/frontend.dockerfile` (`--build-arg APP=<app>`), svelte-adapter-bun.
- **API gateway**: `components/services/gateway` — FastAPI reverse proxy on :8888, longest-prefix `_routes()`, Dapr service-invocation when `RASK_DAPR_ENABLED`, hop-by-hop scrubbing, Location-rewrite, merged OpenAPI `/docs`, tests in `gateway/tests/`.
- **CI/build**: Dagger Go module (`rask/.dagger/` — `migrate-up`, `test-pg`), `.docker/<name>.dockerfile` convention, Alembic migration Job, Tilt loop, k3s make targets, `tests/e2e/mfe.spec.ts` (per-route hydration + `_app` asset-404 + pageerror assertions + gateway round-trip no-internal-redirect check).

## 2. What WE bring that rask lacks entirely

- **Lineage** (`services/lineage` → OpenLineage → Apache AGE graph, column-level `DERIVED_FROM_COLUMN`, `/reconcile`, gold whole-history JSONB, transactional outbox + DLQ (#4), `source_rowid` root provenance (#38a)). rask has zero lineage.
- **The Lance lakehouse catalog** (`services/catalog`, Namespace REST over native `DirectoryNamespace` + dataplane): client-direct writes with vending (#2), per-warehouse bucket control plane + admin API (#3-A, `api/v1/endpoints/warehouses.py`), multi-base data distribution (#3-B), 2.2 + stable-row-ids policy, branches/tags/MVs, model registry + promotion.
- **Actual authz**: rask provisions OpenFGA but wires *nothing* to it (zero `fga` references in `rask/components` or `rask/packages`; datastore is `engine: memory` — ephemeral). We bring `services/common/auth/model.fga` (3 synced files), `catalog/api/fga_deps.py` fail-closed deps, `can_promote`/`can_create_table`/`can_create_warehouse` gates, credential vending (`WebIdentityVendor`), Dex OIDC, the tenancy model (team→project→warehouse→namespace→table). rask also has **no auth at all** ("No auth, no app middleware" — its own CLAUDE.md) and no admin/tenancy API — the control plane is entirely ours.
- **The event-driven medallion estate** (`services/medallion` producer/movers + `services/compaction`) on Dapr pub/sub over NATS JetStream, with resiliency CRs, DLQ parking, stream-provisioning Job, and audit logging.
- **Secrets discipline**: two-tier OpenBao + k8s Secrets, prod-render 0-plaintext guarantee. rask uses bare `existingSecret`.
- **The lineage-explorer frontend** (`frontend/apps/web`: graph via @xyflow/svelte, columns view, models page) — already pre-shaped as Turborepo+bun `apps/web` + `packages/ui` for this graft, but note it is a **BFF**: `routes/api/[...path]` → lineage, `routes/capi/[...path]` → catalog, with its own Dex OIDC session (`lib/server/oidc.ts`) — a different data path than rask's relative-`/api`→gateway pattern.

## 3. The actual integration seams to build

1. **Repo graft** — move `services/{catalog,lineage,medallion,compaction,common}` into rask's Polylith layout (components/services bricks or one vendored group), with *explicit* uv + bun workspace membership (rask globs nothing), `projects/<name>/pyproject.toml` per deployable, ruff line-length 160 / `ty error-on-warning` / pytest importlib alignment.
2. **Chart merge** — fold our first-party templates (`chart/templates/{services,medallion,compaction,dapr-*,nats-stream-job,openfga-migrate,network-policy,dex,openbao,external-secrets}.yaml`) into `rask/chart/templates/`, then flip the already-built externalization values: `rustfs.enabled=false` → Tenant (`add lance-catalog`,`lance-source`,`lance-sink`,`lance-observability` to `rustfs.buckets`), `age.enabled=false` → CNPG (add `lineage`+`openfga` databases — ⚠️ AGE extension decision first), delete `age-postgres/rustfs/backup-*/gateway/web` templates. Both charts pin **identical** subchart versions (nats 2.14.2, dapr 1.18.1, openfga 0.3.9, greptime 0.4.5, vector 0.56.0, perses 0.22.0) so dedupe to one instance of each is a values exercise, not a version fight.
3. **Gateway/route merge** — retire our nginx gateway (`chart/templates/gateway.yaml`); add catalog/lineage/produce upstreams to rask's `gateway/_routes()` (Dapr app-ids + env fallbacks, e.g. `/api/v1/lineage`→lineage, `/api/v1/catalog`→catalog). Must port the nginx `lineageSidecarOnlyRoutes` 403 regex guard (internal Dapr-only routes) into the gateway or the services themselves — it's a security control, not plumbing. `/perses/`+`/greptime/` exposure follows rask's ingress + our #5e auth fix.
4. **OpenFGA productionization in rask** — `openfga.datastore` memory→postgres (CNPG), our `openfga-migrate` model-load Job + seeds, `test_fga_model_contract.py` guard comes along.
5. **MFE graft of our web app** — `frontend/apps/web` → `rask/components/frontends/lineage` (or a `catalog` domain): set `kit.paths.base=/default/lineage` (it currently has **no base** — hard requirement), adopt `@rask/ui/shell` AppShell + add the nav-config entry, register in `microfrontends.json`, root workspaces, `frontend.apps` chart list, ingress path; keep the BFF (`/default/lineage/api/*` server routes carry the OIDC session — base-path-aware URLs needed) rather than forcing `@rask/api`; merge `frontend/packages/ui` (@lance/ui) into `@rask/ui` or demote to app-local; build via the parametrized `frontend.dockerfile`; **add `/default/lineage` to `tests/e2e/mfe.spec.ts` ROUTES** and keep its asset-404/pageerror/gateway-redirect assertions green.
6. **lance-ray on KubeRay** — real Ray Data jobs honoring the frozen seam contract (RASK-INTEGRATION §lance-ray): head emits ONE OpenLineage event and must NOT self-publish `medallion.raw` (double-fire), movers CDF-transform + `DERIVED_FROM` + next trigger, gold embeds lineage JSONB, FGA gates when enabled, workload-identity vending (no durable secret on compute). Transport = `RayJob` CR behind the existing function signatures (OPERATORS §3); mind the known lance_ray↔pylance-8 gaps (no stable-row-ids from `write_lance`, distributed-index fallback).
7. **Image/CI** — one `.docker/<svc>.dockerfile` per our deployable (catalog, lineage, medallion×2, compaction, the web app) + Dagger functions in `rask/.dagger` (build/test/publish), our pytest suites into rask's `make test`/CI.
8. **Auth + secrets seam** — Dex (keep local-only or swap to rask's real IdP), map OpenBao two-tier onto external-secrets-from-Vault for prod, keep app-tier Dapr secret store fail-closed.

## 4. Conflicts / decisions needed (resolve before or in week 1)

- **AGE on CNPG** (blocking the chart flip): custom Postgres-with-AGE image for the CNPG `Cluster` vs separate AGE operand vs the `GOAL-prove-it.md` Lance-native-graph pivot that deletes the dependency.
- **Two charts → one**: rask's umbrella wins (RASK-INTEGRATION already assumes it). Two NATS/Dapr/OpenFGA/observability stacks collapse to one each — but our Dapr surface is richer (pubsub Components, resiliency CRs, app-tokens, secret store, 32Mi body size already matched) and our NATS needs the stream-provisioning Job (LINEAGE/MEDALLION/TRAINING) or NACK; rask's Dapr is sidecar-invocation-only today.
- **Auth posture collision**: rask is trusted-network/no-auth end-to-end; we are fail-closed OIDC+FGA. Decide the blast radius: auth only on our services + our zone's BFF (lowest-friction, ships as-is) vs gateway-wide enforcement (turns on authz for rask's batches/search/volumes — real scope).
- **Duplicate frontends/design systems**: two Bits-UI+Tailwind component libraries (`@rask/ui` vs `@lance/ui`) — merge into `@rask/ui` per RASK-INTEGRATION §5, and decide the domain name for our zone (`lineage` vs folding into rask's dummy `train`/`studio`-style slot or `discover`). Also two e2e Playwright setups → extend rask's `tests/e2e`.
- **Python layout**: restructure our `services/` into Polylith bricks (clean, but touches every import; pythonpath=["services"] vs src-layout packages) vs vendor as-is first and refactor later. Also naming: `RASK_*` env convention vs our `LANCE_*` (rask explicitly bans other prefixes).
- **Observability**: one shared GreptimeDB (recommended; both charts already pin the same stack) vs per-workload — RASK-INTEGRATION open decision #4. Perses dashboards merge either way.
- **Tenancy mapping**: rask's IA is project-first with one implicit `default` project (static bases `/default/<domain>`); our FGA has real projects/warehouses. Keep warehouse-per-deploy + `default` for the merge; multi-project routing is explicitly deferred on both sides.
- **Non-blockers to leave alone**: the 7 catalog 501s (upstream Rust work, parallel track), #20 parked, MV lineage WONTFIX-until-consumer.

## Verification bar (from RASK-INTEGRATION §Verification)
`scripts/governance_e2e.sh` + medallion e2e green **against CNPG + the rustfs Tenant**; `tests/integration/test_spec_conformance.py` green; a real KubeRay run → gold with JSONB lineage + `/reconcile` `in_sync`; `helm template` shows our workloads on `<release>-postgres`/`<release>-rustfs` with no in-cluster DNS leaks and zero plaintext secrets; `mfe.spec.ts` (with `/default/lineage` added) green.

## 3. Production-readiness gaps

# Production-readiness assessment — lance-ns

Method: rendered `helm template lance-ns ./chart -f chart/values.yaml -f chart/values-prod.yaml` with the required prod overrides (image tags, `dapr.appToken`, `age.password`, `rustfs.secretKey`, `observability.edgeAuth.htpasswd`, ingress host) — the fail-closed render guards all work as documented — then inspected the full 5.7k-line manifest plus `chart/values*.yaml`, `chart/templates/*`, `docs/{DURABILITY,OPERATORS,DEPLOY,KIND-RUNBOOK}.md`. Render saved at `/tmp/claude-1000/-home-blackwell-Desktop-lance-ns/88508a85-af5d-44c1-9ef1-92d04ece7015/scratchpad/prod-render.yaml`.

What is already genuinely good: render fails closed on every placeholder prod secret; 0 plaintext secret *values* in env/args (only access-key IDs; secret keys are all `secretKeyRef` → `lance-ns-infra-credentials`); app tier fully hardened (runAsNonRoot, drop-ALL, seccomp, readOnlyRootFilesystem, /livez probes ×42, preStop drain); daprd sidecars resource-bounded; Dapr mTLS pinned on; audit stream ON in prod; `/produce` and `/demo` correctly gated off; PDBs + 2 replicas for the stateless tier; 14d observability TTL; pg-dump + VolumeSnapshot CronJobs exist; `age.externalHost`/`rustfs.externalEndpoint` externalization keys verified actually rewiring env (the values-prod header saying they "need follow-up hooks" is stale doc-drift).

## CRITICAL

1. **Dex is a demo IdP in the prod render** — `chart/templates/dex.yaml`: `storage: type: memory` (sessions lost on restart, hardcoded `replicas: 1`), static users alice/bob with bcrypt("password") hardcoded in the template, static client secret `lance-catalog-secret` in a plain ConfigMap, issuer `http://lance-ns-dex:5556/dex` (in-cluster HTTP — unreachable by real browsers), redirect `http://localhost:8080/callback`. `values-prod.yaml` doesn't touch dex at all, yet the entire governance layer (auth.enabled=true) rests on it. **Unnoticed** (no written deferral found). Fix: prod overlay must set an externally-reachable HTTPS issuer, a real storage backend (dex supports postgres — AGE is already there), connector to the org IdP (or at minimum remove the static demo users), and move the client secret into the infra-credentials/OpenBao path.

2. **No alerting engine at all** — confirmed: zero PrometheusRule/Alertmanager/rule-evaluator anywhere; the only trace is a Perses panel literally titled "alertable pair" (`perses-dashboards.yaml:148`). Nobody is paged when NATS dies, OpenBao seals, the outbox stops draining, or the DLQ fills. **Unnoticed.** Fix: GreptimeDB speaks PromQL (`/v1/prometheus`) — deploy vmalert (or Prometheus in rule-only mode) + Alertmanager with a real route; seed rules from the panels already labeled alertable (outbox depth/oldest-age, DLQ rate, error rate, NATS consumer lag, pod restarts).

3. **NetworkPolicy: zero objects in the prod render** — the full L3 layer (default-deny in/egress, DNS allow, exclusive client lists for openbao/age/rustfs) is *built* in `network-policy.yaml` but `networkPolicy.enabled` stays false and values-prod doesn't flip it. Compounding: the values file itself documents that lance-ray's in-cluster `/produce` ClusterIP route "stays reachable + unauthenticated" — with no L3 layer there is no compensating control against in-cluster cascade forgery. **Documented deferral** (kind CNI doesn't enforce; §7a runbook exists) — but the prod overlay is exactly where the flip belongs. Fix: `networkPolicy.enabled: true` in values-prod (+ `extraEgress` for any externalized backend), and note the policy-enforcing-CNI prerequisite there.

4. **Single-instance data/authz tier = platform-wide SPOF stack** — in the prod render: AGE Postgres 1 replica / 1Gi PVC (values-prod never sizes it) serving BOTH the lineage graph AND OpenFGA's datastore → AGE down = every governed request in the platform fails closed; NATS 1 node / `streamReplicas: 1` / 1Gi (bus down = no lineage ingest, no cascade — the parked **#20**); OpenFGA `replicaCount: 1` (stateless over PG — trivially scalable, not flipped); RustFS 1 replica on one 200Gi PVC = the entire lakehouse + observability store; OpenBao 1. **Documented deferral** ("stateful HA is the operators' job in rask — CNPG/rustfs-operator"; NATS externalize stanza commented in values-prod) — legitimate, but until the rask merge the self-contained prod path has no HA story. Fix now: `openfga.replicaCount: 2` (free), size `age.storage`, and treat the externalize stanzas (managed PG, clustered NATS `streamReplicas: 3`, managed S3) as the actual prod gate, not an optional footnote. Also: the auto-derived external DSNs keep `sslmode=disable` — externalizing PG today would go plaintext unless the openfga DSN is manually overridden.

5. **OpenBao sealed-on-restart = boot deadlock** — devMode=false is correct, but every pod restart/node drain leaves OpenBao sealed until a human runs `bao operator unseal`; apps consume secrets fail-closed via Dapr *at startup*, so app pods hang at "waiting for application startup" (the documented two-sided deadlock, `docs/OPERATORS.md` §5). No auto-unseal. **Documented deferral** (ESO / bank-vaults / vault-operator is "the destination"). Routine prod events (node drains, OOM) trigger it — do the operator adoption before calling this tier prod-ready, and alert on seal status (see gap 2).

## IMPORTANT

6. **Kind-only image assumptions baked into the chart** — app images render as bare local names (`lance-rest-catalog:v1.0.0`, no registry) and the chart has **no imagePullSecrets support anywhere** — unpullable on any real cluster except via node preloading. All images tag-pinned (good), none digest-pinned; one mutable third-party tag in the render (`ghcr.io/cloudoperators/greenhouse-extensions-integration-test:main`, openfga subchart helm-test pod). **Unnoticed.** Fix: registry-qualified `image.*.repository` in values-prod + `imagePullSecrets` plumbing in `_helpers.tpl`/pod specs; digests for the two first-party images.

7. **Resources: one-size-fits-all app tier, unbounded infra tier** — every app container gets `resources.default` (50m/128Mi → 1cpu/512Mi), including RustFS (the whole S3 data plane capped at 512Mi) and AGE Postgres (512Mi). Meanwhile GreptimeDB, NATS, Vector (a DaemonSet — unbounded on every node), OpenFGA, Perses, and the entire Dapr control plane have **no requests/limits at all**. **Unnoticed.** Fix: per-component resource keys for the first-party infra; pass resources through the subchart values (greptimedb-standalone, nats, vector, openfga, dapr).

8. **2-replica services have no spread/anti-affinity** — the only podAntiAffinity in the render belongs to the Dapr scheduler. catalog/lineage/gateway/web (replicas=2, PDB minAvailable=1) can co-schedule on one node → one node failure still takes the service to 0. **Unnoticed.** Fix: `topologySpreadConstraints` on `kubernetes.io/hostname` in the shared pod template.

9. **Dapr control plane left non-HA in prod** — values-prod doesn't set `dapr.global.ha.enabled=true`: operator/sentry/injector/placement all 1 replica. Placement is *hard-required by daprd 1.18 at boot* (your own values comment) — a placement restart during a rollout stalls every new sidecar; sentry down stops mTLS issuance. **Unnoticed** (one-line flip). 

10. **Backups: destination shares fate with the primary, no retention, no restore runbook** — the pg-dump CronJob uploads lineage+openfga dumps to `s3://lance-catalog/_backups/pg/` — the *same RustFS PVC* the VolumeSnapshot protects, so a PVC/cluster loss takes primary and pg backups together; nothing prunes `_backups/` (unbounded growth inside the lakehouse bucket) or old VolumeSnapshots (the CronJob only creates); `snapshotClassName: ""` will fail on clusters without a default class; and there is **no restore procedure anywhere in docs** (grep "restore" in DURABILITY.md backup section = 0 hits). The externalize-to-operators posture is a **documented deferral**, but the retention/fate-sharing/restore gaps in the shipped mechanism are **unnoticed**. Fix: point pg dumps at an off-cluster bucket, prune both artifact kinds, set the class, write and *drill* a restore runbook.

11. **No TLS at the edge** — the rendered Ingress has no `tls:` block (values-prod leaves it empty, no cert-manager annotation): OIDC tokens, the edge-auth basic credentials, and vended S3 credentials would traverse plaintext HTTP. In-cluster, Dapr mTLS covers service-invocation/pubsub only; direct app→infra hops are plaintext (`sslmode=disable`, http RustFS/Greptime) — acceptable in-cluster, but the edge is not. **Unnoticed.** Fix: `ingress.tls` + cert-manager annotation in values-prod.

12. **Built-and-live-proven hardening switches silently omitted from values-prod** — `security.serviceAccounts` (every app pod runs as SA `default` with token automount on), `security.infraContexts`, `dapr.sidecarRestricted` all false in the prod render, despite memory recording them "left ON and proven" on the 2026-07-13 live pass. PSA `restricted` enforce is separately **parked** (Vector hostPath — documented, KIND-RUNBOOK §6.4). The values-prod omission of the proven switches looks like an oversight, not a decision. Fix: flip all three in values-prod (with the documented `dapr_sidecar_injector.sidecarDropALLCapabilities=true` companion).

13. **`deployment.environment.name=kind` ships in the prod render** — values-prod never overrides `observability.environment`; every OTel resource attr will claim prod telemetry is kind. **Unnoticed.** One line.

14. **Manual out-of-band install-order footguns** — values-prod itself warns: flip `medallion.fgaEnabled` without first running `scripts/seed_medallion_fga.sh` and the movers fail closed / pipeline stalls; OpenBao needs manual init+unseal; the namespace PSA label is a manual kubectl step. There is no prod install runbook — DEPLOY.md is kind-framed ("how it all works on kind"), OPERATORS.md is strategy. Fix: a PROD-RUNBOOK.md (ordered: secrets → seed FGA → unseal → flip governance → verify), plus consider a seed Job/hook for the FGA grants.

## NICE

15. `moverReplicas: 1` throughput/availability ceiling — **documented deferral** (process-local single-flight lock; raise after a cross-pod lock ships). Fine as-is; NATS redelivery + idempotence bound the damage.
16. nats-box debug shell Deployment ships in the prod render — set the nats subchart's `natsBox.enabled=false` in values-prod.
17. Dapr scheduler STS: 3×16Gi PVCs for actors/workflows/jobs the stack doesn't use — shrink the PVC size via subchart values.
18. Retention odds: `runRetentionDays: 0` (Run nodes grow forever — prod has the reconcile pruner deployed but the knob off), `compaction.lineageEmit: false` (the compaction FAILURE lineage surface stays dark in prod), `freshnessBudgetHours: 0`. Audit-stream retention sharing the 14d observability TTL is a **documented deferral** — note 14d is short for a compliance trail.
19. HPA off (documented optional, needs metrics-server) — fine; enable once resource requests are truthful (gap 7), since HPA math depends on them.
20. values-prod header's claim that the externalize stanzas "do nothing without follow-up hooks" is stale — verified `age.externalHost` rewires lineage DSN, openfga DSN, wait-init and pg_dump correctly. Fix the comment so operators trust the mechanism.

## Deferred vs unnoticed — the roll-up
- **Written deferrals (don't re-litigate, schedule):** NATS HA/externalization (#20 parked), stateful HA via rask operators (CNPG/rustfs-operator), OpenBao auto-unseal via ESO/bank-vaults (OPERATORS.md §5), audit retention = observability TTL, PSA restricted enforce (Vector hostPath), L3 default-deny known un-flipped (§7a runbook), moverReplicas=1, mode_b vending.
- **Genuinely unnoticed:** Dex demo-IdP posture in prod, no alerting engine, backup fate-sharing/retention/restore, no registry/imagePullSecrets plumbing, unbounded infra resources, no anti-affinity, dapr.global.ha unflipped, values-prod omitting the live-proven SA/infraContexts/sidecarRestricted switches, environment=kind attr, no edge TLS, no prod install runbook.

