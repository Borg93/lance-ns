{
	"name": "@rask/api",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"license": "Apache-2.0",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"default": "./src/index.ts"
		}
	},
	"scripts": {
		"check": "tsgo --noEmit -p tsconfig.json",
		"check:tsgo": "tsgo --noEmit -p tsconfig.json",
		"test": "vitest run"
	},
	"dependencies": {
		"valibot": "^1.4.1"
	},
	"devDependencies": {
		"vitest": "^4.1.9"
	}
}
