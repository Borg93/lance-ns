{
	"name": "@repo/zone-contract",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev:proxy": "bun src/proxy.ts",
		"check:tsgo": "tsgo --noEmit -p tsconfig.json",
		"test": "vitest run",
		"lint": "oxlint .",
		"fmt": "oxfmt .",
		"fmt:check": "oxfmt --check ."
	},
	"devDependencies": {
		"@repo/config": "workspace:*",
		"@types/bun": "^1.3.14",
		"@types/node": "^26.1.1",
		"vitest": "^4.1.10"
	}
}
