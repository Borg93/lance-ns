export { default as StatusBoard } from './status-board.svelte';
// `RunStatusLike` now lives in `$lib/runs` (exported from the package root and from `@repo/ui/runs`)
// — one shape for every surface that reads a lineage run, board and bell alike.
