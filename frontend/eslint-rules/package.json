{
	"name": "@rask/eslint-rules",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"exports": {
		"./cross-zone-reload": "./cross-zone-reload.js"
	},
	"scripts": {
		"test": "vitest run",
		"lint": "oxlint --config ../.oxlintrc.json .",
		"fmt": "oxfmt --config ../.oxfmtrc.json --ignore-path ../.oxfmtignore .",
		"fmt:check": "oxfmt --config ../.oxfmtrc.json --ignore-path ../.oxfmtignore --check ."
	},
	"devDependencies": {
		"vitest": "^4.1.10"
	}
}
